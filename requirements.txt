# ============================================================
# 区块链 AI 量化交易 · 依赖清单
# ============================================================

# 核心数据科学
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
matplotlib>=3.7.0
seaborn>=0.12.0
mplfinance>=0.12.9b0

# ---- 交易所数据（加密市场）----
ccxt>=4.0.0               # 统一交易所API（Binance/OKX/Bybit等100+）
hyperliquid-python-sdk    # Hyperliquid永续合约
websockets>=12.0          # 实时数据流

# ---- 链上数据 ----
web3>=6.0.0               # 以太坊链上交互
# solders                 # Solana（需单独pip安装）
dune-client>=1.0.0        # Dune Analytics API

# ---- 数据存储 ----
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0    # PostgreSQL驱动（TimescaleDB）
redis>=5.0.0              # 实时缓存
h5py>=3.9.0               # 大型数据集存储

# ---- 量化回测 ----
vectorbt>=0.26.0          # 高性能向量化回测（首选）
pandas-ta>=0.3.14b        # 技术指标库
# backtrader>=1.9.78      # 备用回测框架

# ---- 统计分析 ----
statsmodels>=0.14.0
arch>=6.2.0
scikit-learn>=1.3.0

# ---- 机器学习（主力）----
lightgbm>=4.0.0
xgboost>=1.7.0
optuna>=3.0.0             # 超参数优化（贝叶斯搜索）
shap>=0.42.0              # 模型可解释性

# ---- 深度学习 ----
torch>=2.0.0
# pytorch-forecasting>=1.0.0  # Temporal Fusion Transformer
# stable-baselines3>=2.0.0    # 强化学习（PPO/SAC）

# ---- NLP情绪分析 ----
transformers>=4.30.0      # HuggingFace（FinBERT等）
openai>=1.0.0             # GPT API
requests>=2.31.0

# ---- 监控与告警 ----
python-telegram-bot>=20.0 # Telegram机器人告警
grafana-api-client         # Grafana API（可选）
loguru>=0.7.0             # 结构化日志
prometheus-client>=0.18.0 # 指标暴露给Grafana

# ---- 工具 ----
jupyter>=1.0.0
jupyterlab>=4.0.0
python-dotenv>=1.0.0
tqdm>=4.65.0
pydantic>=2.0.0           # 数据验证
apscheduler>=3.10.0       # 定时任务调度
aiohttp>=3.9.0            # 异步HTTP
asyncio                    # 异步并发
